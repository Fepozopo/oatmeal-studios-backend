version: "2"
sql:
  - engine: "postgresql"
    queries: "query"
    schema: "schema"
    gen:
      go:
        package: "db"
        out: "db"
        emit_json_tags: true
        emit_prepared_queries: true
